This is a minimalistic web server that serves you a local index file.

Run your server as follows -

$ gcc -o tserver -pthread threaded_server.c 
$ ./tserver <port number> 

In your browser, go to http://localhost:[port number]
